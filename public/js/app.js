'use strict';


// Declare app level module which depends on filters, and services
angular.module(
	'wheezy',
	[
		'ngCookies'
    ]
).config(
        [
            '$routeProvider',
            function($routeProvider) {

        }
    ]
)