'use strict';


// Declare app level module which depends on filters, and services
angular.module(
		'sprite_util',
		[
			'ngCookies'
		]
	).config(
		[

			function () {

			}
		]
	).run([
		'$document',
		'$rootScope',
		'$timeout',
		function ($document, $rootScope, $timeout) {



		}
	])